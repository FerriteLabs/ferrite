[package]
name = "ferrite-fn-custom-merge"
version = "0.1.0"
edition = "2021"
description = "LWW-by-source-rank merge for Chronicle — Ferrite Forge WASM module"

[lib]
crate-type = ["cdylib"]

[dependencies]
# wit-bindgen = "0.36"  # Uncomment when building with cargo-component
