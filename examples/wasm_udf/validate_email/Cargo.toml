[package]
name = "ferrite-udf-validate-email"
version = "1.0.0"
edition = "2021"
publish = false
description = "Email validation UDF for Ferrite (WASM)"

[lib]
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
