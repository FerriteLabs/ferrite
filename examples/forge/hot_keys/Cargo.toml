[package]
name = "ferrite-fn-hot-keys"
version = "0.1.0"
edition = "2021"
description = "Streaming top-K hot key detector (space-saving) — Ferrite Forge WASM module"

[lib]
crate-type = ["cdylib"]

[dependencies]
# wit-bindgen = "0.36"  # Uncomment when building with cargo-component
