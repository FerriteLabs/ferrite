[package]
name = "ferrite-udf-json-transform"
version = "1.0.0"
edition = "2021"
publish = false
description = "JSON transformation UDF for Ferrite (WASM)"

[lib]
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
